{% extends 'base.html.twig' %}

{% block body %}
    <h3>Concerts à venir :</h3>
    <div class="card-deck">
        <div class="row">
        {% for next_concert in next_concerts %}
            <div class="card">
                <img src="{{ asset('img/concertrandom.jpg') }}" style="height: 125px; ">
                <div class="card-body">
                    <h5 class="card-title">{{ next_concert.tourName }}</h5>
                    <p class="card-text">{{ next_concert.date ? next_concert.date|date('Y-m-d') : '' }}</p>
                    <p class="card-text"><small class="text-muted"><a href="{{ path('concert_show', {'id': next_concert.id}) }}">let's see!</a></small></p>
                </div>
            </div>
        {% endfor %}
        </div>
    </div>
{% endblock %}
